.gradle/
build/
.idea/
*.iml
out/
.kotlin/
local.properties

# CMake build outputs
**/build/native/
CMakeCache.txt
CMakeFiles/
cmake_install.cmake

# vcpkg manifest install tree (libvpx) — created under each module's CMake
# binary dir, regenerated from vcpkg.json on configure.
vcpkg_installed/

# Local test staging (CI artifact downloads + staged natives for local publish)
ci-artifacts/
webp-natives-*/src/main/resources/native/
